<div metal:use-macro="load: ../shared/_layout.pt">
    <div metal:fill-slot="content" tal:omit-tag="True">
        <div class="section content">
            <div class="container spray_program-list">
                <div class="spray-program-list container">
                    <h2 class="title">Spray Programs for ${current_season}</h2>
                    <div class="content">
                        <div tal:condition="not is_viewing_archive">
                            <a href="/spray_program/new" class="button is-link is-small" type="button">
                                + Add Spray Program
                            </a>
                        </div>
                    </div>
                    <div id="message-area"></div>

                    <table class="table is-striped is-hoverable is-fullwidth">
                        <thead>
                            <tr>
                                <th>Name</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr tal:repeat="sp spray_programs" id="spray_row_${sp.id}">
                                <td>
                                    <strong>
                                        <a hx-get="/spray_programs/${sp.id}" hx-target="body" hx-swap="outerHTML"
                                            hx-push-url="true" class="loading-button">
                                            <span class="icon loading-spinner">
                                                <i class="fas fa-spinner"></i>
                                            </span>
                                            <span class="normal-text">${sp.name}</span>
                                            <span class="loading-text">Loading...</span>
                                        </a>
                                    </strong>
                                </td>
                            </tr>
                        </tbody>
                    </table>

                    <div tal:condition="not is_viewing_archive">
                        <div class="is-flex is-justify-content-flex-end mt-4">
                            <button class="button is-warning is-small" hx-get="/spray_seasons/archive" hx-target="body"
                                hx-swap="beforeend">
                                Archive Season &amp; Start New
                            </button>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div metal:fill-slot="additional-css" tal:omit-tag="True">
        <!-- additional css from the page -->
    </div>
</div>